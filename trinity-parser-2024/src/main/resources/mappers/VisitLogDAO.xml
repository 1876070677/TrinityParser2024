<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cuk.api.VisitLog.VisitLogDAO">
    <select id="getVisitLogs" resultType="cuk.api.VisitLog.Entities.VisitLog">
        select id, context, DATE_FORMAT(created_time, "%Y-%m-%d %T") as created_time, visible, isAdmin, likes, (select count(id) from VisitLog) as total_records
        from VisitLog
        <if test="cursor == 0">
            order by created_time desc limit 15;
        </if>
        <if test="cursor != 0">
            where id &lt; #{cursor} order by created_time desc limit 15;
        </if>
    </select>

    <select id="createVisitLog" parameterType="cuk.api.VisitLog.Request.CreateRequest">
        insert into VisitLog(context, isAdmin) values(#{context}, #{isAdmin});
    </select>

    <select id="incrLikes">
        update VisitLog set likes = likes+1 where id=#{id} and likes &lt; 99;
    </select>
</mapper>